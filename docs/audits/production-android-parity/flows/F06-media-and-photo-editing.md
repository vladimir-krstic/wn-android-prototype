# F06 — Media acquisition, editing and reading

## Purpose and current composition

Extend composer attachments, rich message bodies, viewer and Shared Content. External pickers remain system-owned. Draft identity survives edit/preview/cancel, and bytes/URI failures never become a sent success.

Prototype surface: `ui/conversation/ConversationComposer.kt; MediaViewer.kt; model/ComposerModels.kt`. Reuse `WhiteNoiseScaffold`, tonal Settings groups/rows, shared sheets/dialogs, `WhiteNoiseTextField`, `WhiteNoiseButton`, adaptive content bounds, message action/reaction components, MediaViewer and established empty/loading/error content as applicable. Keep 48 dp minimum targets, label-above forms, semantic error colors, bounded state layers, RTL, 200% text, IME/inset ownership and compact/expanded behavior.

## Required content and proposed copy

Use **Gallery**, **Take photo**, **Document**, **Location**, **User**, **Contact**, **Edit photo**, **Crop**, **Rotate**, **Draw**, **Erase**, **Undo/Redo**, **Save**, **Share**, **Open**, and format-specific unavailable/error copy. Never call a device contact a White Noise person.

These labels are the audit recommendation and follow current prototype terminology. Validate exact surrounding help/error copy in the selected screen brief against each matrix source link; preserve production security and destructive consequences without exposing implementation terms.

## Capability and state contract

| Capability | Initial state | Event / Back behavior | Observable result |
| --- | --- | --- | --- |
| C048 · Photo Picker, Files and external camera acquisition | Existing equivalent | Trigger its named entry/action; cancel with Back where available | Preserve behavior |
| C049 · Recent device-media strip and partial permission access | Implemented; host verified within the prototype boundary | Choose, cancel or retry; Back dismisses the inner surface without sending | Recent media exposes none, selected-only, full and unavailable local states with bundled items and named accessible Add actions. Gallery remains the standard Photo Picker. Developer Tools selects the deterministic access state; no real library permission or MediaStore query was added. Q06 retains that production integration outside the authorized prototype scope. |
| C050 · Photo crop, rotate, draw and erase editor | Implemented; host verified | Edit the selected draft/album frame; dirty Back/Close asks to discard; Save commits only on success | Seven crop presets, free adjustment, quarter turns, five ink colors/four widths, ink-only erasing and bounded undo/redo/reset preserve source geometry. Native coordinate alternatives, loading/saving, limits and retry are present. Original images, recipes and per-frame quality are retained in drafts; whole-draft quality replays edits. Source/profile/revision guards prevent stale commits; sibling frames, caption, reply and review exclusion survive. Pixel/EXIF/UI cases compile; device and visual acceptance remain pending. |
| C051 · Per-draft quality selection and metadata policy | Implemented; host verified within the prototype boundary | Choose, cancel or retry; Back dismisses the inner surface without sending | Draft-owned Low/Standard/High/Original choices retain source images for reversible reprocessing, report actual prepared bytes and preserve IDs/caption/reply. High retains the accepted 4096px/95 default. Original strips JPEG/PNG metadata without resizing when supported; orientation/color/unsupported-container cases use a disclosed safe re-encode. Outgoing messages discard retained source images. Cancel/stale-owner/removed-draft results cannot overwrite current work. |
| C052 · Upload/download, cache and failed-transfer states | Implemented; host verified within the prototype boundary | Choose, cancel or retry; Back dismisses the inner surface without sending | Profile/chat/message/attachment-owned queued and active upload/download states carry progress, cancellation, failed/cache-miss retry and terminal expired/invalid/unavailable outcomes. Foreground stepping survives app navigation; system background pauses it. Profile/session changes invalidate outstanding revisions. Viewer/export projections require available bytes; no network transport or service is added. |
| C053 · Image/video viewer, exact page, save and share | Existing equivalent | Trigger its named entry/action; cancel with Back where available | Preserve behavior |
| C054 · Animated image playback | Implemented; host verified within the prototype boundary | Choose, cancel or retry; Back dismisses the inner surface without sending | Bundled 12-frame animated content and picked GIF bytes render through platform decoding on API 28+, stop in the background and respect disabled animations. API 23–27 use a sampled static fallback; corrupt/unavailable content has a named error. GIF sanitation preserves image/control/loop data and removes comments/other application metadata. UI/decoder cases are compiled; device playback remains unverified. |
| C055 · In-app text and Markdown attachment reader | Implemented; host verified within the prototype boundary | Open from a message or Shared Content; Back dismisses the inner surface; profile/source changes invalidate pending work | B13 implemented: Bounded strict UTF-8/BOM UTF-16 text and Markdown reading includes full filename copy, loading/empty/retry, binary/encoding/size failures, visible render truncation with full original Copy, native selection and chunked Read Aloud. External opening reports missing handlers/access failures; profile/source changes revoke pending work. Native speech/system checks remain unexecuted. |
| C056 · Shared-media library categories | Implemented; host verified within the prototype boundary | Open from a message or Shared Content; Back dismisses the inner surface; profile/source changes invalidate pending work | B13 implemented: Existing Shared Content gains All/Images/Videos filters and Voice/audio, newest-first localized month groups, sender/time/unknown-size metadata, body URLs and every album frame. Unavailable rows retain Go to Message; deleted/expired sources disappear. One native foreground audio session supports load/play/pause/seek/error/retry with stale-source guards. Fresh viewer projections and GIF-preserving save/share retain source identity. |
| C057 · Share White Noise person versus device contact | Implemented; host verified within the prototype boundary | Choose, cancel or retry; Back dismisses the inner surface without sending | White Noise person keeps profile identity/navigation. Device contact instead launches the permissionless phone-row picker, reads only that granted row, previews selectable available fields, and queues an escaped UTF-8 vCard plus selected-field text. Unchecked/absent fields are omitted, empty cards cannot send, cancellation retains the ordinary draft, and no phone contact is inferred to be a White Noise identity. Email is represented only when supplied; the system phone-row grant does not authorize email-table queries. |
| C058 · Location selection and location messages | Decision required | Trigger its named entry/action; cancel with Back where available | Add fixed-coordinate selection/review, current-location availability/denial, message card and Maps handoff proposal. Real location/map networking or osmdroid dependency needs Q06. |
| C059 · APK attachment open/install behavior by distribution | Implemented; host verified within the prototype boundary | Open from a message or Shared Content; Back dismisses the inner surface; profile/source changes invalidate pending work | B13 implemented within Q06: MIME/filename and binary-manifest archive-shape checks distinguish invalid packages; local states represent distribution restriction, missing install permission, unavailable installer and ready review. Save file/Close remain available where bytes exist. No installation intent, new permission, actual installation or signature-verification claim; provider-refined APK dispatch is also blocked. |

## Production integration seam

Production evidence for each row is linked in the [matrix](../capability-matrix.md). During prototype work, add the smallest profile-owned immutable fixture/state transitions and callbacks needed to render every named result. Do not add Marmot, networking, signing, persistence, notification delivery, background services or cryptography. Name production events and ownership in the selected screen brief so the eventual migration reconnects to the cited controller/state methods rather than copying prototype fixtures into production storage.

## Copy, accessibility and adaptation

Use the approved product language and terminology. Production strings in the matrix are evidence of meaning, not automatic final copy. Keep raw keys, event IDs, MLS and engine errors off ordinary surfaces; developer surfaces may be exact. State must be conveyed by text/semantics as well as icon/color. Provide accessible equivalents for gestures, logical focus and Back order, and preserve action eligibility at large type and narrow height.

## Acceptance and host validation

- Every linked capability has a deterministic route/fixture and every mutation yields the specified success, cancellation, unavailable and failure outcomes relevant to it.
- Back, profile switching and restored state cannot commit work to the wrong profile/chat or repeat a completed mutation.
- Existing capabilities in this flow retain their current model and UI tests.
- Add unit tests for rules/ownership and Compose tests for durable navigation/actions/semantics. Run targeted host tests while iterating and the repository static gate after a meaningful batch. Compile instrumentation tests only; device execution and visual acceptance require a separate current request.

## Dependencies and decisions

Batches: B11, B12, B13, B14. Decisions: Q06, Q07. Facts are the matrix's cited production behavior and current prototype evidence. UI placement and proposed copy remain recommendations until the selected screen brief records them.
